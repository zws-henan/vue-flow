# git 详情版

## 配置

```bash
git config -l # 查看当前生效的所有配置
git config 键 # 查看指定配置项的值
git config --范围 键 "值" # 配置指定范围的配置项
```

**配置范围**
- worktree：当前工作目录
- global：(常用)对当前的所有用户生效
- system：对所有用户生效的配置
- local ：(常用)(默认)仅对当前仓库生效
- 优先级：worktree > local > global > system

**常见配置项**

- user.name：(必填)用户姓名
- user.email：(必填)用户邮箱
- core.editor：默认的文本编辑器
- core.ignorecase:配置是否忽略文件名的大小写。默认是true，即忽略文件名的大小写。
- init.defaultBranch:配置默认的分支名。

```bash
#配置用户名和邮箱
git config --global user.name "your name"
git config --global user.email "your email"
#配置默认的文本编辑器
git config --global core.editor "code --wait"
#是否忽略文件名的大小写.false表示不忽略
git config --global core.ignorecase false
# 配置默认分支名
git config --global init.defaultBranch main
```

**初始化仓库**

```bash
# 初始化仓库
git init
```

## 暂存和提交

提交是什么？
提交是指将暂存区中的文件提交到本地仓库的过程。
每一次提交都是对工作目录中文件的一个快照。每一个快照就是一个提交

什么时候提交？
- 当工作目录中的文件发生了变化，需要保存到本地仓库时。
- 例如：
  - 新增了新的文件
  - 修改了文件的内容
  - 删除了文件
  - 改变文件的权限
  - 完成了一个功能模块               
  - 修复了一个bug


> 提交分为两步
1. 暂存：将工作目录中的文件添加到暂存区，准备提交。
2. 提交：将暂存区中的文件提交到本地仓库。

**暂存**

```bash
# 查看当前工作目录的状态
git status
# 暂存所有文件
git add .
```

**提交**

```bash
git commit #会打开默认的文本编辑器，要求用户输入提交信息
# 提交暂存的文件。直接在命令行输入提交信息。
git commit -m "提交信息"

git commit --amend -m "提交信息" # 修改上一次提交的信息

```
> 理念：历史不可更改，只能新增。如果需要修改历史记录，只能通过回滚提交来实现。
所以只建议修改最近一次提交的信息。过去的信息尽量不要修改。如果必须修改，建议通过回滚提交来实现。

> 术语：暂存区和工作区
- 暂存区：暂存区是指将工作目录中的文件添加到暂存区，准备提交。
- 工作区：工作区是指用户在本地的目录，用于存储和管理项目文件。

> 提交信息的规范

1. 编写提交信息时不要犯错，多检查。或利用ai工具生成提交信息。
2. 提交提交信息，确保提交信息，确保符合规范。发现消息错误，及时修改。



### 提交信息格式

```bash
<类型>(<范围>): <主题>

<正文>

<页脚>
```

### 提交类型说明

| 类型 | 标识符 | 说明 | 适用场景 | 示例 |
|------|--------|------|----------|------|
| **feat** | feat | 新增功能 | 添加新功能、新特性 | `feat(auth): 添加用户登录功能` |
| **fix** | fix | 修复 Bug | 修复代码缺陷、错误 | `fix(api): 修复用户列表接口返回空数据` |
| **docs** | docs | 文档变更 | 更新文档、注释 | `docs(readme): 更新安装说明` |
| **style** | style | 代码格式 | 不影响代码逻辑的格式调整 | `style: 统一缩进为2空格` |
| **refactor** | refactor | 代码重构 | 重写代码但不改变功能 | `refactor(user): 重构用户模块代码` |
| **perf** | perf | 性能优化 | 提升代码性能 | `perf: 优化图片加载速度` |
| **test** | test | 测试相关 | 添加或修改测试用例 | `test(auth): 添加登录测试用例` |
| **chore** | chore | 构建/工具 | 构建工具、依赖、配置变更 | `chore: 更新依赖版本` |
| **revert** | revert | 回滚提交 | 撤销之前的提交 | `revert: feat(auth): 添加用户登录功能` |
| **build** | build | 构建系统 | 构建流程、CI/CD 配置 | `build: 添加 GitHub Actions 配置` |
| **ci** | ci | CI 配置 | CI 流程、自动化脚本 | `ci: 配置 Jenkins 流水线` |
| **wip** | wip | 工作进行中 | 未完成的工作（临时提交） | `wip(login): 开始实现登录页面` |

### 提交信息编写规则

1. **主题行**：
   - 使用祈使句（动词原形）开头
   - 首字母大写
   - 不超过 50 个字符
   - 结尾不加句号

2. **正文**：
   - 详细描述变更内容
   - 每行不超过 72 个字符
   - 说明为什么做这个变更

3. **页脚**：
   - 关联 Issue 编号：`Closes #123`
   - 注明破坏性变更：`BREAKING CHANGE: ...`
   - 引用相关提交：`See commit abc123`

### 良好示例

```bash
feat(user): 添加用户注册功能

- 添加注册页面组件
- 实现表单验证逻辑
- 调用后端注册接口

Closes #45
```

```bash
fix(api): 修复商品列表接口分页问题

当 page 参数为 0 时，返回空数据而不是第一页
修复 SQL 语句中 LIMIT 参数计算错误

Closes #23
```

```bash
refactor(order): 重构订单模块

- 拆分大函数为多个小函数
- 优化代码结构，提高可读性
- 添加类型定义
```

### 常见错误示例

| 错误示例 | 问题 | 正确示例 |
|----------|------|----------|
| `完成登录页面` | 未使用类型标识符 | `feat(auth): 完成登录页面` |
| `Fix bug` | 类型小写，首字母未大写 | `fix: 修复登录 Bug` |
| `添加了新功能，包括用户注册和登录` | 主题过长 | `feat(auth): 添加用户注册和登录功能` |
| `修改了代码` | 描述过于模糊 | `refactor: 优化用户模块代码结构` |


**查看提交记录**
```bash
# 查看提交记录
git log
# 查看提交记录的详细信息
git log --oneline
```

**回滚提交**
```bash
# 回滚到上一次提交
git reset --soft HEAD^
# 回滚到上一次提交并删除暂存区中的文件
git reset --hard HEAD^
```
> 理念：回滚当然可以回滚，但是这种操作非常危险，建议在必要时使用。实际开发中若要回滚。请使用restore命令。这样更加符合git的规范。
```bash
git restore --source 提交ID # 将指定的提交覆盖到当前工作目录
```
> 注意：要保证工作区干净，（暂存区无未提交的文件，工作区无更改的文件）。

## 分支管理
```bash
# 查看所有分支
git branch

# 重命名分支
git branch -m 旧分支名 新分支名 # 重命名分支。如果只写一个分支名表示修改当前的分支。

# 创建新分支，指针和当前分支指向相同的位置
git branch <branch-name>
#创建一个新的分支，指针指向指定的提交ID
git branch <branch-name> 提交ID

# 切换分支
git switch 分支名 # 切换到指定分支
git switch - # 切换到上一个分支
# 创建并切换到新分支
git switch -c 新分支名

# 删除分支
git branch -d 分支名

# 合并分支
git merge 分支名 # 合并指定分支到当前分支
```

### 分支基本生命周期

> 注意：永远不要在主分支上直接改动，主分支永远保证功能完整可用。可部署到生产环境。
1. 提出需求
   - 新功能，bug修复，文档变更等
2. 新建分支
   - 每个需求对应一个分支
   - feature/*：新功能分支
   - bugfix/*：bug修复分支
   - hotfix/*：紧急修复分支
   - release/*：发布分支
   - chore/*：构建/工具分支
   - docs/*：文档变更分支
   - refactor/*：代码重构分支
   - perf/*：性能优化分支
   - test/*：测试相关分支
   - build/*：构建系统分支
   - ci/*：CI 配置分支
   - wip/*：工作进行中分支
   - revert/*：回滚提交分支
3. 在分支上开发

4. 合并分支到主分支

5. 主分支测试

6. 删除开发分支

**保留现场**

当你在一个新分支上开发时，这是需要紧急修改一个bug，需要创建一个hotfix分支。但是当前分支上还有未提交的更改，你不能直接合并到主分支。可以使用`git stash`命令将未提交的更改保存起来。
```bash
git stash
```
> 注意：`git stash`命令将未提交的更改保存到一个临时的stash中，你可以稍后再合并到主分支。
```bash
git merge hotfix # 合并hotfix分支到当前分支
```
> 注意：合并后，你需要删除hotfix分支。
```bash
git branch -d hotfix # 删除hotfix分支
```

##  多账号的ssh配置规范

```bash
ssh-keygen -t rsa -C "your_email@example.com" -f ~/.ssh/zws_github_key

```

进入github账号的ssh配置页面，添加zws_github_key.pub文件的内容。

[github账号ssh配置页面](https://github.com/settings/ssh)

**配置密钥配对的规则**


## 远程分支的同步

> 注意：在本地分支上开发时，需要先同步远程分支到本地分支。

1. 在github上创建远程仓库
   - 远程仓库地址：https://github.com/your_username/your_repository.git
2. 在本地仓库中添加远程仓库的别名

```bash
# 查看当前远程仓库的别名
git remote -v

# 添加远程仓库的别名
git remote add 远程仓库别名 远程仓库ssh地址

# 修改远程仓库的别名
git remote rename 原别名 新别名

```

**分支同步**

1. push同步
   - 将本地分支的更改推送到远程仓库
   - 语法：git push -u  远程仓库别名 分支名
   - 示例：git push origin main
   - 只要推送过一次，后续的push操作都可以省略-u参数及后续参数
   - 示例：git push
   - 例如：你在github上创建了一个仓库，你需要将本地仓库的更改推送到远程仓库。你可以执行`git push origin main`命令(之前配置过远程仓库的别名)。这样子就可以同步本地仓库的更改到远程仓库。
2. fetch同步
   - 从远程仓库拉取更改到本地仓库
   - 语法：git fetch 远程仓库别名 分支名
   - 示例：git fetch origin main
   - 例如：你在github上要拉去一个仓库的main分支，你需要执行`git fetch origin main`。然后你可以使用`git merge origin/main`命令将远程分支的更改合并到本地分支。这样子，你就可以在本地分支上开发了。

3. pull 命令
   - 可以一步替代fetch和merge命令，即先fetch，再merge。merge时，会自动解决冲突。
   - 语法：git pull 远程仓库别名 分支名
   - 示例：git pull origin main
   - 例如：你在github上要拉去一个仓库的main分支，你需要执行拉取和合并。这个命令会自动执行fetch和merge命令。可以更加方便地同步远程仓库的更改到本地仓库。

4. clone 命令
   - 从远程仓库克隆一个本地仓库
   - 语法：git clone 远程仓库ssh地址
   - 示例：git clone https://github.com/your_username/your_repository.git

## github flow


本地开发者规范
1. 本地的main永远只读，不能直接改动。者能够拉取远程分支的更改。即pull main分支。
2. 基于最新的main分支创建新的分支。

管理员规范
1. main分支只能通过pull request合并。
2. pr永远无冲突

## 协作者与非协作者 flow

协作者的flow与上述一致。

非协作者这是有一些前期准备的不同。
1. fork要想要的仓库到自己的账号下。
2. 从自己的账号下克隆仓库到本地。
3. 添加远程仓库源
```bash
# 添加上游

git remote add upstream https://github.com/原作者/仓库名.git

# 查看远程仓库的别名(确认fork是你的fork，upstream是原作者的仓库)
git remote -v
```
